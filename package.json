{
    "name": "root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "node backend/server.js",
        "heroku-postbuild": "cd backend && npm install && cd ../front-end && npm install && npm run build"
    },
    "dependencies": {
        "dotenv": "^16.4.5",
    },
    "engines": {
        "node": ">=22.8.0"
    }
}